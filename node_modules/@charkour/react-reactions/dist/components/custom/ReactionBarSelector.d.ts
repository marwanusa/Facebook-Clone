import React from 'react';
import { Reaction } from '../../helpers';
export interface ReactionBarSelectorProps {
    iconSize?: number;
    reactions?: Reaction[];
    onSelect?: (key: string) => void;
    style?: React.CSSProperties;
}
export declare const ReactionBarSelector: React.ForwardRefExoticComponent<ReactionBarSelectorProps & React.RefAttributes<HTMLDivElement>>;
export declare const defaultProps: Required<ReactionBarSelectorProps>;
export default ReactionBarSelector;
